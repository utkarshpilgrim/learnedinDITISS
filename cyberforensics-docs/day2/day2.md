# Initial Assessment

The First Job of the initial assessment is to secure the scene, meaning when the incident is found to happen, administrators first job is to inform the seniors and experts, tell them about the incident. **Secure the scene** by preventing any alteration to the digital and physical evidence. 

# Practical Implementation of Hexadecimal Code

- It is used in graphical color coding, in web devlopment the *hex codes* are used in order to assign colors. 

- It is also used in file dumps, when doing forensics on the file, we use hexadecimal editors that will give the file contents in hexadecimal. 

```
Offset (Hex) | Bytes (Hex) | ASCII
-----------------------------------
00000000     | 48 65 6C 6C 6F 00 | Hello.
```

- It is used for finding the memory dumps, coverting the binary code to more readable format, and analyze the raw data. 



# Preservation

All the entities like harddisk, pendrives, SD cards and all those evidence need to be tagged, there should be documentation for all the entitites, make a hash for all the data, whether pendrive, harddrive and all other stuffs needs to be hashed properly with multiple has algorithms. 

Check for default configuration settings for the logs, check whether they have the override settings. Send these logs to some different location, from where you can access them, because these logs are going to be covered or deleted, meaning it is important to store the logs to multiple locations. All the logs needs to be preserved in isolated environment

# Identification

A crime always leaves lots of evidences, meaning if a fake account is created, all you need to check is within the browser history, what kind of things you should be searching is what you need to consider. If there are hacking scenarios, there would be traffic that you'll be looking for, you must have the logs for the traffic, inspecting all those logs and identifying the suspicious IPs. 

***What, where, which is all the question you need to look***, for example, logs are deleted, what kind of history you should be looking for, is what needs to be inspected. 

# Extraction



# Documentation

# Interpretation

# `dd`: Copy Hard Disk Partition

Use `dd` command to copy the partition of the hard disk. 

# FTK Imager

# NBNS Protocol



Refer to [Virus Total](https://www.virustotal.com/gui/) website that can be used in order to detect whether the file you've downloaded is infected or not. 

***HTTPS secure service, how does it work?***, Also tell how does the ***SSH works, when you initiate a connection from your machine?***

# Encoding & Encryption

Encoding is used for the representing the data into differnt format, whereas Encryption is used for the protection of the data, where we want to protect the configdentiality of the data. 

# The Hex Editor

What is hex editor, it is used in cyber forensics in order to analyze the **raw data**, it allows editing at file level, bit level, and byte level as well depending on the requirement. 

Hex editors are used in debugging program crashes, to identify at what point the prgaram is crashing, if null bytes are found something like `FF FF FF FF`, that would mean the code is crashing because of this user input. 

Hex codes are also used for the data recovery, 
