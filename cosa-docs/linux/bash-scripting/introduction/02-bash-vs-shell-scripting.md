# Better call it Shell Scripting using Bash

To be practical, we can say that there are multiple shells that are derived since 1979, all the shells are derivate of shell developed by Stephen Bourne. For example, `zsh`, `bash`, `dash` shells and many more. 

To be practical again, we can say, *many shells are out there which we are using for our scripting depends*, say we are using bash, zsh, and all, but mostly we are using bash shell for our scripting, so to put in nice words, we can prevent the differences between shell scripting and bash scripting by say **shell scripting using bash**. 

```zsh
❯ LS -lA shells.png
-rw-r--r--  1 utkarshsingh  staff  2204349 Dec 30 18:48 shells.png
```

We can talk about the differences for the individual shell, like zsh shell is more backward compatible with bash shell, it automatically take care of auto-correction and capitalisation mistakes. 